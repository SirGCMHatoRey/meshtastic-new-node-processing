## Agent skills

### Issue tracker

Issues live as GitHub issues (uses `gh` CLI). Remote: `SirGCMHatoRey/meshtastic-new-node-processing`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five canonical labels used as-is: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.
